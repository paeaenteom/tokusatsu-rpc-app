[한국어](CHANGELOG.md) · [English](CHANGELOG.en.md) · **日本語**

# 変更履歴

バージョンごとに何が変わったかを残しています。新しいものが上です。

---

## 0.3.1 beta — 2026-08-21

### 新機能 — 表示のしかたを選べます
- **アクティビティの種類** — 視聴中 / 再生中 / プレイ中
  - Discord は種類ごとにカードの描き方が違います。視聴中は進行バーが出て、
    再生中はメンバー一覧にも画像とタイトルが並びます
- **メンバー一覧の表示** — 名前の下の一行に何を出すかを**サイトごと**に
  (作品名 / エピソード / アプリ名)

### 修正
- **設定を変えても何も起きなかった問題** — 原因が二つ重なっていました
  - 選んだ値がそもそも Discord に**送られていませんでした。** 接続ライブラリが
    自分の知る項目だけ残して他を捨て、その後に既定値が入っていました
  - 値を保存するだけでカードを描き直していませんでした。種類だけでなく
    **作品名表示・時間表示などすべての表示設定**が同じでした
- **ライブ配信を認識できなかった問題**
  - プレーヤーがまだライブと分かっていない瞬間、画面のライブ表示を無視していました
  - 一度「ライブでない」と決まると後で確定しても直らず、進行バーのままでした
    (実測: 2 時間の進行バーとして表示)
- **消したはずのカードが復活する問題** — 接続前に保留したカードが取り消されず、
  タブを閉じた後も YouTube・Disney+ の表示が残っていました
- **TTFC の続きから再生で作品名がサイト名になる問題**
  - 作品名をエピソード一覧でしか覚えておらず、続きから再生は一覧を飛ばします。
    今は再生画面から直接読みます
- 視聴中に言語を変えると閲覧中のカードとして描かれていた問題
- 進行バーのずれ記録がライブ配信で出続けていた問題

### そのほか
- アプリが突然消えたときに原因が分かるよう、終了・エラーの記録を残します
  (この PC 内にのみ保存し、どこにも送信しません)
- YouTube のページ監視を必要な間だけに絞りました

---

## 0.3.0 beta — 2026-08-21

### 反応速度
- **タイトルとチャンネル名がずっと早く出ます** (YouTube)
  - 画面(DOM)の更新を待たずにプレーヤーへ直接尋ねます。
    実測: 移動後 **340ms** (画面が埋まるのは 1.1〜2.5 秒後)
  - 移動直後に**前の動画のタイトル・チャンネルが一瞬見えていた**のも解消しました。
    今見ている動画の情報が確実になるまで送りません
- **再生・一時停止が即座に反映されます** — 実測平均 **32ms** (反応速度優先オン時)

### 時間表示の正確さ
- 進行バーの基準点を**画面から読んだその瞬間**に計算します
  - 以前はアプリが「今 − 受け取った秒」で作っており、小数が切り捨てられ転送の遅れも
    加わって、更新のたびに位置がずれていました。TTFC・IMAGINATION・YouTube すべてに適用
- 総再生時間が実際より 1 秒長く出ていたのを直しました
- まだ再生できない状態(バッファリング等)では基準点を作りません。
  そこで作ると、実際に再生が始まるまでの分だけカードが先に進んでいました

> 残る **±1 秒**は消せません。Discord は時刻を秒単位でしか受け取らないため、
> カードは時計の秒で、プレーヤーは動画の秒で切り替わります。
> 毎秒の一部でずれて見えます (現在の設定が最も一致します)。

### 修正
- **経過時間が最初の値で止まり**、進行バーが毎回先頭に戻っていた問題
- 接続が閉じる瞬間に**未処理エラーが記録されていた問題** (二か所)
- チャンネルページで**チャンネル名が二重に表示されていた問題**
- 移動直後にタイトルが一瞬 **「YouTube」** になっていた問題

### そのほか
- 記録に診断行を追加しました — 進行バーが実際と 2 秒以上ずれたら残します

### 知っておくこと
- YouTube の再生情報を得るため、**ページと同じ空間で動くスクリプト**が一つ増えました
  (`extractors/youtube-main.js`)。再生位置とタイトルを尋ねるだけで、ページは変更しません。
  Disney+ も同じ方式です
- **0.2.10 は公開されていません。** 0.2.9 からの方は下の 0.2.10 の内容
  (YouTube 対応・Discord 接続の安定化) も今回まとめて受け取ります

---

## 0.2.10 beta — 2026-08-19

### 新機能
- **YouTube 対応**
  - 動画タイトル・チャンネル名・経過時間・サムネイル
  - **ショート**
  - **ライブ配信** — 配信が始まった時刻から経過時間が伸びます。
    途中から見ても最初からの時間が表示されます
  - **チャンネルページ**ではそのチャンネルのプロフィール画像を表示します
  - 閲覧中: ホーム・検索・登録チャンネル・再生履歴・再生リスト・チャンネル

### 修正 (すべてのサイトが対象)
- **アプリを起動すると Discord 接続が自ら切れて繋ぎ直していた問題**
  - 最初の接続にログインを二重に掛けており、捨てられた側が 10 秒後に失敗して
    正常だった接続まで切っていました
  - 実測: 起動 10 秒で切断 → 20 秒で再試行 → 30 秒で再接続。現在は 0 回です
- **起動直後に見ているとカードがなかなか出なかった問題**
  - 接続が整う前に届いた最初の状態を捨てていたため、30 秒ごとの更新が来るまで
    プロフィールが空のままでした
- **サムネイルが無い動画で 5 秒ごとに取得を繰り返していた問題**
  - 失敗回数で諦める仕組みがありましたが、アプリの復旧要求が毎回その回数を
    0 に戻していて一度も働いていませんでした

---

## 0.2.9 beta — 2026-08-18

### 修正
- 0.2.8 の Discord 再接続修正で **エラーが記録されていた問題**
  - 接続を破棄する処理は非同期なのに同期的に囲っていたため、閉じたソケットへの
    書き込みが未処理エラーとして漏れていました。動作に影響はありませんが記録が汚れていました

---

## 0.2.8 beta — 2026-08-18

### 修正
- **時間が経つと Discord 接続が切れて戻らない問題**
  - 再接続のたびに古い接続を破棄せず新規作成しており、ハンドルが積み上がって
    Discord に拒否されていました — **再試行するほど悪化する**構造でした
  - 再試行間隔も広がるようにしました (10 秒〜最大 2 分)。接続できれば 10 秒に戻ります
- **別のウィンドウを前面にすると表示が消える問題**
  - Chrome はブラウザが覆われただけでもそのタブを「非表示」とみなします。そのため
    このアプリのウィンドウや Discord を見た瞬間に表示が消えていました
  - 視聴中は覆われても消しません。閲覧中のみ、15 秒以上覆われたままなら消します。
    タブを閉じた場合は従来どおり即座に消えます

### 新機能
- **自動解除までの時間を選べます** — ミニウィンドウの「時間表示」の下
  - 解除しない / 5 分 / 30 分 / 1 時間 / 3 時間 / 6 時間 / 12 時間 / 24 時間 (既定 5 分)

### 画像が速く出ます
- 一時的な失敗のあと画像が **1 分以上**出ない問題を修正
  - 失敗を回数で数えていたため、反応優先 (ブースター) では **0.9 秒**で諦め、その
    ページの間ずっと再試行しませんでした → 時間間隔で判断するようにしました
  - 復旧要求の間隔も 20 秒 → 5 秒
  - TTFC のサムネイルは復旧要求がどのタブにも届かず、実質復旧手段がありませんでした
- アップロードする画像を小さくしました (**容量約 44% 減**)

### 一気見の通知
- 添付画像が**元の比率**に戻りました (プロフィールのカードは正方形のまま)

---

## 0.2.7 beta — 2026-08-15

### ウイルス対策ソフトの誤検知への対応
- Windows Defender がインストーラーを `Trojan:Win32/Sabsik.EN.B!ml` と判定し、
  **ダウンロードが自動削除される問題**に対応しました (誤検知 — `!ml` は機械学習による推測の意)
- **実行ファイルに正体を明記しました。** インストーラーは製作者・製品名・バージョンが
  すべて空 (0.0.0.0) で、アプリ本体は Electron の既定値のまま「GitHub, Inc.」と
  名乗っていました。署名が無いうえ情報も空だと判定に不利です
- **インストール不要の圧縮版も併せて公開します** — `TOKU-RPC-<バージョン>-portable.zip`。
  実行ファイルではないためこの判定を受けません。展開して `TOKU RPC.exe` を実行するだけです
- README とリリースノートに、なぜ起きるのかと報告方法を書きました

> 根本的な解決はコード署名証明書です。個人プロジェクトのためまだありません。

---

## 0.2.6 beta — 2026-08-15

### 修正
- **動画を見てから戻ると、その動画が表示され続ける問題**
  - 戻り先が直前と同じページだと「変化なし」と判断して更新を飛ばしていました。
    画面には動画のカードが出ているのにです
  - 実測: 戻ってから **44 秒間**そのままで、タブを切り替えてようやく解消しました
- **アプリのアイコンが無い問題** — ビルド方法によってアイコンを埋め込む工程が
  丸ごと抜けていました。どの方法でビルドしても必ず入るようにしました
- タブを閉じるとき同じ通知を 2 回送っていたのを 1 回にしました

### アップロードが減ります
- **アプリを再起動しても、既にアップロード済みの画像は上げ直しません**
  - 従来は起動のたびにキャッシュが空になっていました
- 保持期間を保存先に合わせました (72 時間保持されるものを 2 時間で捨てて上げ直していました)

---

## 0.2.5 beta — 2026-08-15

### メモリ
- **トレイ常駐 315MB → 171MB**
  - ウィンドウを閉じると描画用のプロセスを実際に破棄します。以前は隠すだけで、
    トレイにいる間もずっと保持していました (実測 95MB)
  - 開き直すとその場で作り直します。速度と動作は変わりません
- 画像の元データをサイトの数だけ重複保持していたのを 1 つにまとめました

### 画像が速く出ます
- **同じ画像を 3 回アップロードしていたのを 1 回に** (サイトごとの接続が別々に上げていました)
- 合成画像を PNG ではなく JPEG で送ります (透明部分が無いので PNG は不要でした)。
  容量は約 1/4
- 画像の取得順を修正しました。必ず失敗する経路を先に試してから成功する方へ回っており、
  その無駄足がそのまま待ち時間でした

### Disney+
- **一覧・コレクションページでも作品ロゴ**を表示します (作品ページのみだったのを拡大)
- 背景アートワークを高解像度で取得します (400x225 → 1920x1080)。
  一部のページではそもそも取得できていませんでした

### 整理
- 使われなくなったアップロード経路を削除しました (動作の変化はありません)

---

## 0.2.4 beta — 2026-08-14

### Disney+ の作品ページがページそのままの見た目に
- 背景アートワークの上に**作品ロゴ**を重ねて表示します (Disney+ のヒーロー表示と同じ形)
- ロゴだけだと横に非常に長く (4.5:1)、カード上では細い帯になっていました。
  背景を敷くことで正方形を使い切りつつロゴも読めるようにしました
- どちらか一方が無くても表示は壊れません
  → ロゴ+背景なら合成 / ロゴのみ / 背景のみ / どちらも無ければサイトロゴ

### そのほか
- Disney+ の画像 CDN をもう 1 つ許可しました (一部の画像が別のホストから来ます)

---

## 0.2.3 beta — 2026-08-14

### サムネイルが見切れなくなりました
- Discord は大きい画像を**正方形に切り取って**描きます。そのため 16:9 の
  サムネイルは左右 43% が失われ、中央だけが残っていました
- これからは元の画像を丸ごと正方形に収めて送ります (上下の余白は透明)
  → **TTFC・IMAGINATION・Disney+ すべて**画面のまま表示されます

### 反応速度
- 変化があれば**待たずにすぐ** Discord に反映します
  - 以前は変化のたびに 0.5 秒 (閲覧中は 1 秒) を必ず待っていました。
    **ブースターを入れても体感が変わらなかった原因がこれです** —
    拡張機能だけ速くなり、アプリが毎回同じ時間を足し直していました
  - Discord の制限 (20 秒に 5 回) は今まで通り守ります。連打時だけ間隔を空けます
- Disney+ の再生/一時停止をプレーヤーのイベントで即座に取得します (以前は最大 0.4 秒遅れ)

### 修正
- **タブを閉じてもステータスが残る問題** — タブを閉じる処理が存在しない変数を
  読んで停止し、よりによって「消すべきそのとき」だけ通知が出ませんでした
  - 再接続時に閉じたタブの情報が復活する問題も直しました
  - アプリ側にも安全網を入れました。拡張機能からの連絡が 5 分途絶えると自ら片付けます
    (30 秒ごとの更新が既存の安全網をリセットし続け、一度も発動していませんでした)
- **ひとつのサイトを閉じると他のサイトまで消えていた問題**
- **消えたあと同じ画面に戻っても再表示されない問題** (一時停止中)

### Disney+
- 作品ページでその作品の**ロゴ**を表示します (ロゴがない作品は背景アートワーク)

---

## 0.2.2 beta — 2026-08-14

### 新機能 (すべてミニウィンドウの「アプリ設定」で切り替え)
- **新バージョンの通知** — GitHub のリリースを 6 時間ごとに確認し、新しければ知らせます
  - オフにすると確認自体を行いません (通信も発生しません)
  - 「更新を確認」ボタンはオフでも動きます
  - ダウンロードやインストールはしません。知らせて、押すとリリースページを開きます
- **ログオン直後に起動** — Windows が起動アプリを遅らせる待ち行列を回避します
  - タスク スケジューラのログオン トリガーを使います (管理者権限は不要)
  - オンにすると従来の自動起動登録は削除します (二重起動の防止)
- **ブースター** — 状態をより頻繁に見て反応を速くします。CPU を少し多く使います

### 修正
- **Discord に接続していなかった問題** — 対応サイトを開くまで接続を試みてすらおらず、
  「未接続」と表示されていました (0.2 で入った不具合)
- **タスクバーのアイコン**が Electron の既定アイコンになっていた問題
  (ウィンドウとアプリを結ぶ AppUserModelId が未設定でした)
- ミニウィンドウのスクロールバーを非表示にしました

---

## 0.2 beta — 2026-08-13

### Disney+ 対応 *(実験)*
- 作品名・シーズン/話数・エピソード名・経過時間・**エピソードのサムネイル**
- ブランドページ (Disney · PIXAR · MARVEL · STAR WARS · National Geographic)
- 専用の Discord アプリケーションを使います — 問題が起きても TTFC・IMAGINATION は無事です

### 複数のサイトを同時に表示
- これまではひとつしか表示されず、どれを出すか決めきれずちらついていました
- 開いているサイトがすべてプロフィールに並びます。同じサイトを複数タブで開いた
  ときのちらつき防止はそのままです

### メモリと反応速度
- トレイ常駐中 **310MB → 177MB** (ウィンドウを開くまで作りません)
- ウィンドウを閉じている間の毎秒の再描画とログ送信を止めます
- 状態が変わったら次の周期を待たずに反映します

### 修正
- 韓国語でホーム画面が Discord に何も表示されなかった問題
  (Discord は 2 文字未満の文言を受け付けません)
- トレイアイコンが潰れて見えた問題 (その大きさ用のアイコンを使うように)
- アプリの exe にアイコンが入らなかった問題

---

## 0.1 beta — 2026-08-09

最初の公開版。

- 視聴中に **作品名・エピソード・経過時間・サムネイル**を Discord に表示
- 東映特撮ファンクラブ・TSUBURAYA IMAGINATION に対応
- **一気見モード** — 最後まで見ると自動で次の話へ、全画面も維持
  - 一時停止では終わりません
- 一気見の記録を Discord のウェブフックで送信 (任意)
- PC の起動時にトレイへ自動常駐
- **한국어 · English · 日本語**
- アプリと拡張機能をまとめて入れるインストーラー
