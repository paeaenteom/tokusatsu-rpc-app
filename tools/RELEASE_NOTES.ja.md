0.2.8 で記録されていたエラーを解消しました。機能の変更はありません。

## インストール

下の **`TOKU-RPC-Setup-0.2.9-beta.exe`** ひとつだけダウンロードして実行してください。
アプリとブラウザ拡張機能がまとめてインストールされます。ほかにダウンロードするファイルはありません。

- インストール画面で **拡張機能を入れるブラウザにチェックを入れる**だけです
  (Chrome · Edge · NAVER Whale · Brave · Vivaldi)
- 拡張機能を登録するときに **ユーザー アカウント制御 (UAC) のダイアログが一度だけ**表示されます → **[はい]**
  (Windows が拡張機能のポリシー領域への書き込みを管理者にしか許可していないためです。アプリ本体は自分のアカウントのフォルダにインストールされます)
- インストールが終わったら **ブラウザを完全に終了してから起動し直してください**
  (トレイアイコンからも終了させないと拡張機能が反映されません)

## 0.2.9 での変更

### 修正
- 0.2.8 の Discord 再接続修正で **エラーが記録されていた問題**
  - 接続を破棄する処理は非同期なのに同期的に囲っていたため、閉じたソケットへの
    書き込みが未処理エラーとして漏れていました。動作に影響はありませんが記録が汚れていました

## 0.2.8 での変更

### 修正
- **時間が経つと Discord 接続が切れて戻らない問題**
  - 再接続のたびに古い接続を破棄せず新規作成しており、ハンドルが積み上がって
    Discord に拒否されていました — **再試行するほど悪化する**構造でした
  - 再試行間隔も広がるようにしました (10 秒〜最大 2 分)。接続できれば 10 秒に戻ります
- **別のウィンドウを前面にすると表示が消える問題**
  - Chrome はブラウザが覆われただけでもそのタブを「非表示」とみなします。そのため
    このアプリのウィンドウや Discord を見た瞬間に表示が消えていました
  - 視聴中は覆われても消しません。閲覧中のみ、15 秒以上覆われたままなら消します。
    タブを閉じた場合は従来どおり即座に消えます

### 新機能
- **自動解除までの時間を選べます** — ミニウィンドウの「時間表示」の下
  - 解除しない / 5 分 / 30 分 / 1 時間 / 3 時間 / 6 時間 / 12 時間 / 24 時間 (既定 5 分)

### 画像が速く出ます
- 一時的な失敗のあと画像が **1 分以上**出ない問題を修正
  - 失敗を回数で数えていたため、反応優先 (ブースター) では **0.9 秒**で諦め、その
    ページの間ずっと再試行しませんでした → 時間間隔で判断するようにしました
  - 復旧要求の間隔も 20 秒 → 5 秒
  - TTFC のサムネイルは復旧要求がどのタブにも届かず、実質復旧手段がありませんでした
- アップロードする画像を小さくしました (**容量約 44% 減**)

### 一気見の通知
- 添付画像が**元の比率**に戻りました (プロフィールのカードは正方形のまま)

## 0.2.7 での変更

### ウイルス対策ソフトの誤検知への対応
- Windows Defender がインストーラーを `Trojan:Win32/Sabsik.EN.B!ml` と判定し、
  **ダウンロードが自動削除される問題**に対応しました (誤検知 — `!ml` は機械学習による推測の意)
- **実行ファイルに正体を明記しました。** インストーラーは製作者・製品名・バージョンが
  すべて空 (0.0.0.0) で、アプリ本体は Electron の既定値のまま「GitHub, Inc.」と
  名乗っていました。署名が無いうえ情報も空だと判定に不利です
- **インストール不要の圧縮版も併せて公開します** — `TOKU-RPC-<バージョン>-portable.zip`。
  実行ファイルではないためこの判定を受けません。展開して `TOKU RPC.exe` を実行するだけです
- README とリリースノートに、なぜ起きるのかと報告方法を書きました

> 根本的な解決はコード署名証明書です。個人プロジェクトのためまだありません。

## 0.2.6 での変更

### 修正
- **動画を見てから戻ると、その動画が表示され続ける問題**
  - 戻り先が直前と同じページだと「変化なし」と判断して更新を飛ばしていました。
    画面には動画のカードが出ているのにです
  - 実測: 戻ってから **44 秒間**そのままで、タブを切り替えてようやく解消しました
- **アプリのアイコンが無い問題** — ビルド方法によってアイコンを埋め込む工程が
  丸ごと抜けていました。どの方法でビルドしても必ず入るようにしました
- タブを閉じるとき同じ通知を 2 回送っていたのを 1 回にしました

### アップロードが減ります
- **アプリを再起動しても、既にアップロード済みの画像は上げ直しません**
  - 従来は起動のたびにキャッシュが空になっていました
- 保持期間を保存先に合わせました (72 時間保持されるものを 2 時間で捨てて上げ直していました)

## 0.2.5 での変更

### メモリ
- **トレイ常駐 315MB → 171MB**
  - ウィンドウを閉じると描画用のプロセスを実際に破棄します。以前は隠すだけで、
    トレイにいる間もずっと保持していました (実測 95MB)
  - 開き直すとその場で作り直します。速度と動作は変わりません
- 画像の元データをサイトの数だけ重複保持していたのを 1 つにまとめました

### 画像が速く出ます
- **同じ画像を 3 回アップロードしていたのを 1 回に** (サイトごとの接続が別々に上げていました)
- 合成画像を PNG ではなく JPEG で送ります (透明部分が無いので PNG は不要でした)。
  容量は約 1/4
- 画像の取得順を修正しました。必ず失敗する経路を先に試してから成功する方へ回っており、
  その無駄足がそのまま待ち時間でした

### Disney+
- **一覧・コレクションページでも作品ロゴ**を表示します (作品ページのみだったのを拡大)
- 背景アートワークを高解像度で取得します (400x225 → 1920x1080)。
  一部のページではそもそも取得できていませんでした

### 整理
- 使われなくなったアップロード経路を削除しました (動作の変化はありません)

## 0.2.4 での変更

### Disney+ の作品ページがページそのままの見た目に
- 背景アートワークの上に**作品ロゴ**を重ねて表示します (Disney+ のヒーロー表示と同じ形)
- ロゴだけだと横に非常に長く (4.5:1)、カード上では細い帯になっていました。
  背景を敷くことで正方形を使い切りつつロゴも読めるようにしました
- どちらか一方が無くても表示は壊れません
  → ロゴ+背景なら合成 / ロゴのみ / 背景のみ / どちらも無ければサイトロゴ

### そのほか
- Disney+ の画像 CDN をもう 1 つ許可しました (一部の画像が別のホストから来ます)

## 0.2.3 での変更

### サムネイルが見切れなくなりました
- Discord は大きい画像を**正方形に切り取って**描きます → 16:9 のサムネイルは
  左右 43% が失われ、中央だけが残っていました
- これからは元の画像を丸ごと正方形に収めて送ります (上下の余白は透明)
  → **TTFC・IMAGINATION・Disney+ すべて**画面のまま表示されます

### 反応速度 — ほぼ即時
- 変化があれば**待たずにすぐ**反映します
  - 以前は変化のたびに 0.5 秒 (閲覧中は 1 秒) を必ず待っていました。
    **ブースターを入れても体感が変わらなかった原因がこれです** —
    拡張機能だけ速くなり、アプリが毎回同じ時間を足し直していました
  - Discord の制限 (20 秒に 5 回) は今まで通り守ります。連打時だけ間隔を空けます
- Disney+ の再生/一時停止をプレーヤーのイベントで即座に取得します (以前は最大 0.4 秒遅れ)
- ブースターは安全網の役割になりました。切ってもほとんど変わらず、CPU を節約できます

### 修正
- **タブを閉じてもステータスが残る問題** — タブを閉じる処理が存在しない変数を読んで
  停止し、よりによって「消すべきそのとき」だけ通知が出ませんでした
  - 再接続時に閉じたタブの情報が復活する問題も直しました
  - アプリ側の安全網を追加: 拡張機能からの連絡が 5 分途絶えると自ら片付けます
- **ひとつのサイトを閉じると他のサイトまで消えていた問題**
- **消えたあと同じ画面に戻っても再表示されない問題** (一時停止中)

### Disney+
- 作品ページでその作品の**ロゴ**を表示します (ロゴがない場合は背景アートワーク)

## 0.2.2 での変更

### 新機能 — すべてミニウィンドウの「アプリ設定」で切り替えます
- **新バージョンの通知** — GitHub のリリースを 6 時間ごとに確認して知らせます
  - オフにすると確認自体を行いません (通信も発生しません)
  - ダウンロードやインストールはしません。知らせて、押すとリリースページを開きます
- **ログオン直後に起動** — Windows が起動アプリを遅らせる待ち行列を回避します
  - タスク スケジューラを使います。管理者権限は不要です
- **ブースター** — 状態をより頻繁に見て反応を速くします。CPU を少し多く使います

### 修正
- **Discord に接続していなかった問題** — 0.2 では対応サイトを開くまで接続を
  試みてすらおらず、「未接続」と表示されていました
- タスクバーのアイコンが Electron の既定アイコンになっていた問題
- ミニウィンドウのスクロールバーを非表示にしました

バージョンごとの履歴は [CHANGELOG](https://github.com/paeaenteom/tokusatsu-rpc-app/blob/main/CHANGELOG.ja.md) にあります。

## 0.2 での変更

### Disney+ 対応 *(実験)*
- 作品名・シーズン/話数・エピソード名・経過時間・**エピソードのサムネイル**を表示
- ブランドページ (Disney · PIXAR · MARVEL · STAR WARS · National Geographic) にも対応
- まだ検証中のため専用の Discord アプリケーションを使います。ここで問題が起きても
  TTFC・IMAGINATION の表示には影響しません

### 複数のサイトを同時に表示
- これまでは複数開いてもひとつしか表示されず、どれを出すか決めきれずちらついていました
- 開いているサイトがすべてプロフィールに並びます。同じサイトを複数タブで開いたときの
  ちらつき防止はそのままです

### メモリと反応速度
- トレイ常駐中のメモリが **310MB → 177MB**。ウィンドウを開くまで作らないようにしました
- ウィンドウを閉じている間の毎秒の再描画とログ送信を止めます。開けばすぐ最新になります
- 状態が変わったら次の周期を待たずに Discord へ反映します

### そのほか
- タスクバーのアイコンが Electron の既定アイコンになっていた問題を修正
- トレイアイコンが潰れて見えた問題を修正 (その大きさ用のアイコンを使うように)
- 韓国語でホーム画面が Discord に何も表示されなかった問題を修正
  (Discord は 2 文字未満の文言を受け付けません)

## 主な機能

- 視聴中に **作品名・エピソード・経過時間・サムネイル**を Discord に表示
- 見ているサイトに応じて Discord アプリケーションを自動で切り替え
  (TTFC / IMAGINATION / Disney+)
- **一気見モード** — 動画を最後まで見ると自動で次の話へ、全画面も維持
  - 一時停止しても一気見は終わりません
- 一気見の記録を Discord のウェブフックで送信 *(任意 — README 参照)*
- PC の起動時にトレイに自動で常駐
- **한국어 · English · 日本語** — 初期値はシステムの言語
  - アプリはミニウィンドウ、拡張機能はポップアップで変更。拡張機能はアプリに追従します
  - Discord に表示される文言と一気見の記録も同じ言語になります

## ファイルについて

| ファイル | 説明 |
|---|---|
| `TOKU-RPC-Setup-0.2.9-beta.exe` | **これだけダウンロードすれば OK です** (アプリ同梱) |
| `toku-rpc-extension.crx`, `update.xml` | 拡張機能の自動インストール・アップデート用 — ブラウザが自動で取得します |
| `toku-rpc-extension.zip` | 拡張機能の手動インストール用 (自動で入らないときだけ) |
| `TOKU-RPC-<バージョン>-portable.zip` | インストール不要の圧縮版 (ウイルス対策が上の exe を止めるとき) |


## ウイルス対策ソフトが「ウイルスが検出されました」と言うとき

**誤検知です。** コード署名証明書を購入していない個人開発者のインストーラーではよくあることです。

Windows Defender が付ける名前は `Trojan:Win32/Sabsik.EN.B!ml` で、末尾の **`!ml` は
「機械学習による推測」**という意味です。既知のマルウェアと一致したわけではなく、
*署名が無い・自己解凍する・レジストリを触る・管理者権限を求める*という組み合わせが
統計的に怪しく見えるという判定です。インストーラーなら当然行うことばかりです。

**信用できなければ入れなくて構いません。** 確認・回避の方法は次のとおりです。

- **ソースはすべて公開されています** — [リポジトリ](https://github.com/paeaenteom/tokusatsu-rpc-app)には
  インストーラー自体のコード (`tools/installer/Installer.cs`) も含まれています
- **圧縮版を使ってください** — `TOKU-RPC-<バージョン>-portable.zip` は実行ファイルではないため
  この判定を受けません。展開して `TOKU RPC.exe` を実行するだけです
  (拡張機能は `toku-rpc-extension.zip` から手動で登録)
- **Microsoft に誤検知を報告** — [こちら](https://www.microsoft.com/en-us/wdsi/filesubmission)に
  提出すると数日で解除されるのが普通です

署名証明書があれば根本的に解決します。個人プロジェクトのためまだありません。

### 「スマート アプリ コントロール」にブロックされるとき

これは上のウイルス誤検知とは**別の問題**で、現時点では回避手段がありません。

スマート アプリ コントロール (Windows 11 専用) は、**署名の無い実行ファイルを無条件に遮断**します。
評判が積み上がっても、ファイル情報を埋めても通りません。Microsoft 信頼ルート プログラムの
CA が発行したコード署名証明書で署名されたプログラムだけを許可します。

このプログラムにその証明書はありません。個人が買うには安くなく (年 3 万円程度〜)、
Microsoft が薦める安価な代替の Azure Artifact Signing は
**個人開発者の登録が米国・カナダ在住者に限定**されています。

選択肢は 2 つだけです。

- **インストールしない** — 最も安全で、それで構いません
- **スマート アプリ コントロールを切る** — *Windows セキュリティ → アプリとブラウザーの制御 → スマート アプリ コントロール*
  ⚠ **一度切ると、Windows を再インストールするまで元に戻せません。** プログラム 1 つのために
  システム全体の保護を恒久的に下げることになります。見合うかどうか必ず検討してください。
  [ソースはすべて公開しています](https://github.com/paeaenteom/tokusatsu-rpc-app)ので、読んでご自分で判断いただけます。

証明書を用意でき次第、署名済みで配布します。
## 注意点

- Windows 専用です
- 署名されていないプログラムなので SmartScreen の警告が出ることがあります
  → *詳細情報 → 実行*
- インストールログは `%TEMP%\toku-rpc-install.log` に残ります (問題が起きたらこのファイルを確認してください)
- このツールは視聴情報を表示するだけで、動画をダウンロードしたり著作権保護を回避したりはしません
